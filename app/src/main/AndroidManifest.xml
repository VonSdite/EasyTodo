<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="xyz.wendyltanpcy.myapplication">

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.CAMERA"/>
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name="org.litepal.LitePalApplication"
        android:allowBackup="true"
        android:icon="@mipmap/icon2"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".TodoList.MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".FinishList.FinishEventListActivity"
                  android:label="FinishEvent"
                  android:theme="@style/AppTheme.NoActionBar">

        </activity>
        <activity
            android:name=".TodoList.EventContentActivity"
            android:theme="@style/eventContentTheme">

        </activity>
        <activity android:name=".TodoList.EditMenuActivity">
        </activity>
        <activity android:name=".TodoList.EventListActivity"/>
        <activity android:name=".TodoList.SettingsActivity"/>

        <provider
            android:name="android.support.v4.content.FileProvider"
            android:authorities="applicationId.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"/>
        </provider>

        <activity android:name=".TodoBrowser.BrowserActivity">
        </activity>

        <receiver android:name=".Receiver.AlarmReceiver">
            <intent-filter>
                <action android:name="intent_alarm"/>
                <action android:name="intent_event"/>
            </intent-filter>
        </receiver>
    </application>

</manifest>